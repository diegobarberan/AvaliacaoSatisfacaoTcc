@model AvaliacaoDesempenho.Models.Disciplina

@{
    ViewData["Title"] = "Detalhes da Disciplina";
}

<h1>Detalhes da Disciplina</h1>

<div>
    <h4>Disciplina</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@Model.Codigo">Editar</a> |
    <a class="btn btn-outline-primary" asp-action="Index">Voltar para Lista</a>
</div>
